<div class="sidebar">
  <% if user_signed_in? %>
    <nav class="sidebar-nav">
      <ul class="nav">
        <li class="nav-item">
          <%= link_to raw("#{icon 'speedometer'} Courses"), root_path, class: 'nav-link' %>
        </li>
      </ul>
    </nav>
  <% end %>
</div>